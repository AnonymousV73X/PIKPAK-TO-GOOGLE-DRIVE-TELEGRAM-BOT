# Core web app
Flask>=3.0
psutil>=5.9

# The Cloudflare tunnel integration (cloudflare_tunnel.py / run.py) only uses
# the Python standard library — the cloudflared binary itself is downloaded
# on first run. No extra pip packages are required for that.
